## 1. 可选 RAG 基础能力

- [x] 1.1 先为 RAG 默认配置、配置校验、禁用状态、配额关系、模型 Profile 兼容性和强制指定不可用运行后端编写失败测试，再实现 Pydantic RAG 配置 Schema 使测试通过。
- [x] 1.2 增加 CPU `rag` 可选依赖组和各平台加速 Extra，并验证未安装任何 RAG 依赖时基础包仍能正常导入和运行。
- [x] 1.3 定义严格类型化的 RAG ID、请求上下文、文档/Chunk/任务状态、来源位置、证据/搜索结果、安全错误类别和依赖 Protocol，并补齐严格类型测试。
- [x] 1.4 增加 `RagManager` 的条件构造和生命周期管理，证明禁用和不可用状态不会改变现有 Agent、附件、会话或渠道行为。

## 2. 可信身份与私聊范围

- [x] 2.1 为稳定且经过认证的发送者身份，以及 `private/group/public/unknown` 会话类型编写契约测试，再扩展 Channel Base 能力 API。
- [x] 2.2 实现主体派生、带领域分隔的路径哈希、不可变请求上下文和失败即关闭授权，并覆盖主体覆盖攻击测试。
- [x] 2.3 为 Telegram、Discord、Slack 和 WebSocket/WebUI Adapter 实现并测试私聊类型与文档附件能力报告。
- [x] 2.4 为 Feishu、DingTalk、QQ 和 NapCat Adapter 实现并测试私聊类型与文档附件能力报告。
- [x] 2.5 为 Weixin、WeCom 和 MoChat Adapter 实现并测试私聊类型与文档附件能力报告。
- [x] 2.6 为 Matrix、Mattermost、Signal、WhatsApp、MS Teams 和 Email Adapter 实现并测试私聊类型与文档附件能力报告；无法提供可信私聊范围的渠道必须明确失败即关闭。

## 3. 结构化且有界的文档解析

- [x] 3.1 为结构化文本块、检测格式、来源位置、截断/安全状态、空文本和安全错误编写解析契约测试；实现结构化提取 API，并通过 Adapter 保持现有当前轮次字符串行为。
- [x] 3.2 增加 PDF Fixture，实现页码感知提取；拒绝 OCR-only 内容，明确报告页数、内容流、字符和超时限制，不能把前缀标记为完整文档。
- [x] 3.3 增加 DOCX 与 PPTX Fixture，实现标题/幻灯片感知提取，并覆盖 OOXML 压缩包、表格、组合 Shape、加密、宏和内嵌图片安全行为。
- [x] 3.4 增加 XLSX Fixture，实现工作表/行感知、只读显示值提取，并限制行数、单元格、解压大小，禁止公式和宏执行。
- [x] 3.5 为每种支持的文本扩展名增加 Fixture，实现编码安全、行号感知的提取，以及不能通过改扩展名绕过的 MIME/内容校验。
- [x] 3.6 实现有界 Parser Worker Process、墙钟超时、崩溃隔离和清理，并通过测试证明恶意文件不会阻塞 asyncio AgentLoop。

## 4. 按主体隔离的存储、配额与代次

- [x] 4.1 编写存储迁移测试，实现按主体 SQLite Schema 的版本化创建，覆盖文档、Chunk、FTS、任务、配额预占和 Store Manifest。
- [x] 4.2 使用经过清理的系统 ID 实现托管原始文件、工作目录和向量目录，保证权限安全路径与原子文件发布，并覆盖路径穿越测试。
- [x] 4.3 编写并发配额测试，实现 Immediate Transaction 批次预占、已提交用量、失败释放、删除释放、全局存储上限和最小剩余磁盘检查。
- [x] 4.4 实现流式 SHA-256 原始文件入库；测试同一主体完全重复、同名不同内容和跨主体独立拥有相同内容。
- [x] 4.5 编写 USearch Repository 测试，实现不可变版本化 HNSW 代次构建、Manifest 激活、Reader 固定、旧代次回收和 Vector/Chunk 一致性校验。
- [x] 4.6 为 SQLite/向量不一致编写崩溃和损坏测试，再实现启动协调与安全重建路径。

## 5. 切片与中英文词法索引

- [x] 5.1 为段落、标题、页面、幻灯片、工作表、表格和超大文本块编写确定性切片测试；实现 300–400 Token、约 50 Token 重叠且保留来源位置的 Chunk。
- [x] 5.2 实现 E5 Query/Passage 前缀、受 Tokenizer 约束的输入上限、切片/分析器版本签名，并测试标题与位置上下文不会超过模型限制。
- [x] 5.3 增加中文、英文、中英混合、数字、文件名和标识符 Fixture；实现文档与查询共用的固定版本双语词法分析器。
- [x] 5.4 在规范化词法文本上实现 FTS5 索引和 BM25 搜索，但只返回原始证据文本；验证中文精确词和混合标识符可检索。

## 6. 固定版本的本地 CPU 推理

- [x] 6.1 为 `intfloat/multilingual-e5-small` 和 `BAAI/bge-reranker-base` 创建不可变 Manifest，包含产物哈希、Tokenizer、Pooling、归一化、维度、精度、许可证、固定样例输出和标定阈值字段。
- [x] 6.2 先编写缓存完整性与并发测试，再实现加锁下载到临时路径、哈希校验、模型缓存原子发布、离线错误和管理员预取。
- [x] 6.3 为真实与 Fake Embedder 编写契约测试；实现 ONNX CPU 查询与批量 Embedding、正确的 E5 Pooling/归一化、批处理、取消边界和 Profile 签名。
- [x] 6.4 为真实与 Fake Reranker 编写契约测试；实现 ONNX CPU Pair 打分、批处理、规范化分数、标定阈值和有界并发。
- [x] 6.5 验证模型加载始终禁用远程仓库代码，并且任何 Embedding 或 Reranker 失败路径都不能调用第三方推理服务。

## 7. 硬件感知的运行时优化

- [x] 7.1 编写确定性 Hardware Probe 测试，实现操作系统、架构、CPU、内存、加速器、已安装 Execution Provider、驱动/运行时和模型版本指纹。
- [x] 7.2 仅使用已安装且兼容的 Provider，为 CPU、CoreML、CUDA、OpenVINO 和 DirectML 构造候选；验证强制模式且不在运行时安装包。
- [x] 7.3 编写基准选择测试，实现有界预热，以及独立的单查询 Embedding、批量 Embedding 和 Reranker 基准，默认总预算 60 秒、单候选 10 秒。
- [x] 7.4 实现 Embedding 余弦、Reranker 排序/分数、内存和稳定性门禁；验证更快但不兼容的加速器会输给较慢但有效的候选。
- [x] 7.5 按完整硬件/运行时/模型指纹缓存不同工作负载的 Profile 选择，并在状态中展示结果。
- [x] 7.6 编写加速器失败测试，实现候选黑名单、一次性回退事件、安全重试和最终 CPU 回退。
- [x] 7.7 实现 Embedding Profile 兼容检查；模型产物或量化变化时后台分阶段重建索引，只有执行设备变化且校验通过时复用，Reranker 则可直接切换。

## 8. 持久化入库与删除任务

- [x] 8.1 编写状态机转换测试，实现持久化的 queued、parsing、chunking、embedding、indexing、ready、failed 和 deleting 阶段，并记录安全错误和时间戳。
- [x] 8.2 实现启动任务恢复、最多两次的临时错误退避重试、永久错误分类、阶段产物校验和过期工作目录清理。
- [x] 8.3 实现完整 `/rag add` Service：全收或全拒校验与预占、原始文件复制、解析、切片、本地 Embedding、暂存索引、校验、就绪发布和配额提交。
- [x] 8.4 编写长时间入库并发测试，实现有界批次 Yield 和交互式 Embedding/Reranker 优先调度。
- [x] 8.5 实现删除流程：立即排除检索，随后持久删除原始文件、Chunk、FTS 和向量，释放配额，失败可重试，并覆盖未知/其他主体 ID 隐私测试。
- [x] 8.6 实现持久化状态与列表查询，包含分页/保留策略、配额报告、运行 Profile 报告，且不返回托管主机路径。

## 9. Hybrid 检索、证据与评测

- [x] 9.1 增加排名融合 Fixture，实现默认 40 条 BM25 与 40 条 Dense 召回、RRF 融合、Chunk 去重和活动代次过滤。
- [x] 9.2 实现前 30 个融合候选的本地重排、文档多样性、六条证据上限、标定阈值，以及类型化的证据/无证据/不可用/降级结果。
- [x] 9.3 编写检索失败测试，实现明确的仅词法降级策略、损坏或不兼容 Dense 索引排除和披露元数据。
- [x] 9.4 为 PDF 页码、DOCX/HTML/Markdown 标题路径、PPTX 幻灯片、XLSX 工作表/行范围和文本行号实现准确证据序列化与引用格式。
- [x] 9.5 实现不可信证据上下文包装，并通过测试证明文档指令不能设置主体、获得工具权限或向远程主模型暴露未选语料。
- [x] 9.6 创建版本化中英文混合检索语料、答案与无答案标签、评测 Runner、阈值标定输出，以及 BM25-only、Dense-only、Hybrid 对比。
- [x] 9.7 运行固定发布 Profile 评测，仅调整已记录的检索配置，直到 Recall、证据、误命中、引用、基线和零泄漏阈值全部通过。

## 10. 命令与 Agent 工具集成

- [x] 10.1 编写 Command Router 测试，实现 `/rag add`、`/rag status [job_id]`、`/rag list` 和 `/rag delete <document_id>`，包含私聊范围校验和可操作的不可用提示。
- [x] 10.2 实现 `/rag ask <question>` 强制检索，向答案上下文加入引用，并明确处理无证据结果。
- [x] 10.3 通过工具发现机制增加 `search_knowledge_base`，不暴露主体参数，并从当前轮次绑定不可变的服务端请求上下文。
- [x] 10.4 编写 AgentLoop 测试，覆盖自主检索、未检索轮次、主体覆盖尝试、远程主模型证据最小化和引用指令。
- [x] 10.5 更新命令帮助、配置参考、部署/离线模型准备、配额、隐私、支持格式、加速器和故障排查文档。

## 11. 类型化进度事件与渠道展示

- [ ] 11.1 编写事件模型测试，实现 `RagProgressEvent`、安全序列化、回退文本、脱敏、去重和操作/阶段/状态校验。
- [ ] 11.2 为查询开始（500 毫秒内）、入库阶段、删除、模型准备、硬件基准、选中加速、降级、回退、完成和失败实现 Manager/Job 事件发布。
- [ ] 11.3 扩展渠道协调，使 RAG 事件按进度处理且交付为尽力模式；可编辑渠道更新一条消息，纯文本渠道使用最少反馈。
- [ ] 11.4 增加 WebSocket 事件投影和 React/TypeScript WebUI 状态组件或紧凑时间线，并覆盖更新、完成折叠、失败、重连和可访问性测试。
- [ ] 11.5 增加跨渠道事件契约测试，证明限频可防止刷屏、通知失败不改变任务状态，事件不包含文档文本、证据、模型缓存路径或托管路径。

## 12. 验证与发布门禁

- [ ] 12.1 运行 RAG 类型、身份、解析、存储、切片、词法检索、本地推理、硬件 Profile、任务、检索、命令、工具、事件、渠道和 WebUI 的专项单元与集成测试，并修复全部失败。
- [ ] 12.2 运行完整 Python 测试与覆盖率门禁、`ruff check nanobot/`，以及安装所有渠道依赖后的严格 `basedpyright`，修复所有回归。
- [ ] 12.3 运行 WebUI 测试和生产构建，验证事件展示与现有聊天行为。
- [ ] 12.4 在 Windows、macOS 和 Linux 上运行 CPU-only 真实模型冒烟测试，并记录解析、Embedding、索引大小、检索和 Reranker 基准。
- [ ] 12.5 在可用硬件上运行 Apple Silicon、NVIDIA、Intel/OpenVINO 和 Windows GPU 冒烟测试，记录选中 Profile，并确认无效或更慢候选正确回退。
- [ ] 12.6 严格校验 OpenSpec 变更，确认每个场景都有自动化或明确发布验证覆盖，并准备评审但不归档变更。
